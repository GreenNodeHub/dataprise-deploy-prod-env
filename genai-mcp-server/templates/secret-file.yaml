{{- if .Values.fileSecretContent }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.existingSecretFile }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
type: Opaque
data:
  {{- range $k, $v := .Values.fileSecretContent }}
  {{ $k }}: {{ $v }}
  {{- end }}
{{- end }}